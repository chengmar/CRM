# Empty content catalog

This directory intentionally contains no product, market, specification, or campaign content.

The content-pilot architecture remains available in `src/acquisition/content-pilot-catalog.ts`. A new catalog must be created from current product-owner inputs, reviewed, and explicitly approved before it can be staged. Publication and external writes remain disabled by default.
